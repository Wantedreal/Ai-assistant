node_modules/
dist/
.venv/
venv/
__pycache__/
*.pyc

# PyInstaller build cache (regenerated on every build)
backend/build/
backend/dist/

# Electron installer output — large binary artifacts, not source
frontend/release/

# Excel lock files (created when file is open in Excel)
~$*.xlsx
~$*.xls

# Test artifacts
test_report.pdf

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Environment variables
.env

# Per-machine config (stores local Excel source path)
backend/data/import_config.json

# Claude Code session data
.claude/
